name: container
on: [push, pull_request]
jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v4
   - run: docker build -t ung-navstar:test .
   - run: docker run -d --name navstar -p 8000:8000 ung-navstar:test
   - run: sleep 3 && curl --fail http://127.0.0.1:8000/health
